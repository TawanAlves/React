<template>
  <a-modal
    title="审批"
    :width="600"
    :visible="visible"
    @ok="() => { $emit('ok') }"
    @cancel="() => { $emit('cancel') }"
    ok-text="提交"
    cancel-text="取消"
    :keyboard="false"
    :maskClosable="false"
  >
    <a-form :form="form" :label-col="{ span: 4 }" :wrapper-col="{ span: 19 }" >
      <a-form-item label="复查审批">
        <a-radio-group v-decorator="['status', { rules: [{ required: true, message: '请选择复查意见'}]}]">
          <a-radio value="0">通过</a-radio>
          <a-radio value="1">驳回</a-radio>
        </a-radio-group>
      </a-form-item>
      <a-form-item label="复查意见">
        <a-textarea :rows="4" v-decorator="['reviewOpinion', { rules: [{ required: true, message: '请填写复查意见'}]}]"></a-textarea>
      </a-form-item>
    </a-form>
  </a-modal>
</template>

<script>
export default {
  name: 'RecheckModal',
  props: {
    visible: {
      type: Boolean,
      default: false
    }
  },
  data () {
    return {
      form: this.$form.createForm(this)
    }
  }
}
</script>
