<template>
  <a-list>
    <a-list-item :key="index" v-for="(item, index) in historyDatas">
      <div class="list-content">
        <div class="list-content-item">
          <span>{{ item.userTypeName }}</span>
          <p></p>
        </div>
        <div class="list-content-item">
          <span v-if="item.operateStatus == 1 || item.operateStatus == 0">审批意见</span>
          <p v-if="item.operateStatus == 1 || item.operateStatus == 0">{{ item.suggest }}</p>

        </div>
        <div class="list-content-item">
          <span>提交人</span>
          <p>{{ item.submitter }}</p>
        </div>
        <div class="list-content-item">
          <span v-if="item.operateStatus != null">操作类型</span>
          <p v-if="item.operateStatus != null">{{ item.operateName }}</p>
        </div>
        <div class="list-content-item">
          <span>提交时间</span>
          <p>{{ item.submitTime }}</p>
        </div>
        <div class="list-content-item" style="text-align: center">
          <sign-img :src="item.signature"></sign-img>
        </div>
        <div class="list-content-item" style="text-align: right" v-if="false">
          <a>查看图片</a>
        </div>
      </div>
    </a-list-item>
  </a-list>
</template>

<script>
import SignImg from './SignImg'
export default {
  name: 'ApproveList',
  components: { SignImg },
  props: {
    historyDatas: {
      type: Array,
      default: () => [],
      required: true
    }
  }
}
</script>

<style scoped lang="less">
  .list-content-item {
    display: inline-block;
    vertical-align: middle;
    font-size: 14px;

    span {
      font-size: 14px;
      font-weight: 600;
      line-height: 20px;
    }

    p {
      margin-top: 4px;
      margin-bottom: 0;
      line-height: 22px;
    }

  }

  .ant-list-item {
    width: 100%;

    .list-content {
      width: 100%;
      display: flex;
      justify-content: space-between;

      .list-content-item {
        width: 15%;
      }
    }
  }
</style>
