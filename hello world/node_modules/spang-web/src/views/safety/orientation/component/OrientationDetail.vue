<template>
  <a-modal
    title="详细信息"
    :bodyStyle="{maxHeight:'700px',overflow:'auto',paddingTop: '5px'}"
    :width="700"
    :visible="visible"
    :closable="false"
    @cancel="() => { $emit('cancel') }"
    :keyboard="false"
    :maskClosable="false"
  >
    <template slot="footer">
      <a-button type="primary" @click="() => { $emit('cancel') }">返回</a-button>
    </template>
    <a-card style="margin-top: 8px">
      <a-descriptions :column="1" style="margin-left: 80px" :colon="true" v-if="mode === 'certificate'">
        <a-descriptions-item label="人员">{{ info.userName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="部门">{{ info.deptName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="性别">{{ info.sex || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="身份证">{{ info.idCard || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="证书名称">{{ info.certificateName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="发证机关">{{ info.certificateAuthority || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="证书编号">{{ info.certificateCode || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="有效开始日期">{{ info.effectiveBgDate || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="有效结束日期">{{ info.effectiveEndDate || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="复审日期">{{ info.reviewDate || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="特种作业种类">{{ info.certificateTypeName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="适用的作业票">{{ info.workTitcketName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="原件存放">{{ info.storageAddress || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="备注">{{ info.remark || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="附件">{{ info.file || ' - ' }}</a-descriptions-item>
      </a-descriptions>
      <a-descriptions :column="1" style="margin-left: 80px" :colon="true" v-if="mode === 'safetyeducation'">
        <a-descriptions-item label="员工">{{ info.personName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="培训课时">{{ info.trainingPeriod || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="授课开始日期">{{ info.startTime || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="授课结束日期">{{ info.endTime || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="考试成绩">{{ info.score || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="授课人">{{ info.lecturer || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="教育类型">{{ info.educateTypeName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="创建时间">{{ info.createTime || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="创建人">{{ info.createUsername || ' - ' }}</a-descriptions-item>
      </a-descriptions>
      <a-descriptions :column="1" style="margin-left: 80px" :colon="true" v-if="mode === 'plan'">
        <a-descriptions-item label="名称">{{ info.trainingName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="执行类型">{{ info.executeTypeName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="计划开始时间">{{ info.startTime || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="培训主管单位">{{ info.competentUnitName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="创建时间">{{ info.createTime || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="责任人">{{ info.chargerName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="状态">{{ info.statusName || ' - ' }}</a-descriptions-item>
      </a-descriptions>
      <a-descriptions :column="1" style="margin-left: 80px" :colon="true" v-if="mode === 'record'">
        <a-descriptions-item label="名称">{{ info.trainingName || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="编号">{{ info.trainingNumber || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="培训形式">{{ info.trainingType || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="培训开始时间">{{ info.startTime || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="培训结束时间">{{ info.endTime || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="培训地点">{{ info.trainingAddress || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="主讲人">{{ info.keynoteSpeaker || ' - ' }}</a-descriptions-item>
        <a-descriptions-item label="学时">{{ info.trainingPeriod || ' - ' }}</a-descriptions-item>
      </a-descriptions>
    </a-card>
  </a-modal>
</template>

<script>
export default {
  name: 'OrientationDetail',
  props: {
    visible: {
      type: Boolean,
      required: true
    },
    info: {
      type: Object,
      default: {}
    },
    mode: {
      type: String,
      default: ''
    }
  }
}
</script>
<style lang="less" scoped>
/deep/ .ant-modal-title {
  font-size: 18px;
}
/deep/ .ant-card-head-title {
  font-weight: bolder;
}
/deep/ .ant-descriptions-item-label {
  width: 150px;
  font-size: 15px;
  //font-weight: bolder;
  text-align: right;
}
/deep/ .ant-descriptions-item-content {
  font-size: 15px;
  font-weight: bolder;
}
</style>
