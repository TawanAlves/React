<template>
  <page-container :title="false">
    <a-card :bordered="false" style="margin: 15px;">
      <a-card
        :bordered="false"
        :tab-list="tabList"
        :active-tab-key="tabKey"
        @tabChange="key => tabChange(key, 'tabKey')"
        v-if="false"
      />
      <div v-if="tabKey==='tab1'"><personnel /></div>
      <!--    <div v-else-if="tabKey==='tab2'" ><region/></div>-->
      <div v-else-if="tabKey==='tab3'"><danger-source /></div>
    </a-card>
  </page-container>
</template>

<script>
import Personnel from '@/views/safety/organizations/Personnel'
import Region from '@/views/safety/organizations/Region'
import DangerSource from '@/views/safety/organizations/DangerSource'

export default {
  name: 'Index',
  components: {
    Personnel,
    Region,
    DangerSource
  },
  data () {
    return {
      tabKey: 'tab1',
      tabList: [
        {
          key: 'tab1',
          tab: '人员管理'
        },
        // {
        //   key: 'tab2',
        //   tab: '区域管理'
        // },
        {
          key: 'tab3',
          tab: '危险源管理'
        }
      ]
    }
  },
  methods: {
    tabChange (key, type) {
      // this.$message.success(`${ type }`)
      this[type] = key
    }
  }
}
</script>
