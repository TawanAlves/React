<template>
  <page-container :title="false">
    <a-card :bordered="false">
      <div class="table-page-search-wrapper">
        <a-form layout="inline">
          <a-row :gutter="24">
            <a-col :md="6">
              <a-form-item label="条件1">
                <a-input />
              </a-form-item>
            </a-col>
            <a-col :md="6">
              <a-form-item label="条件2">
                <a-input />
              </a-form-item>
            </a-col>
            <a-col :md="1">
              <span class="table-page-search-submitButtons">
                <a-button type="primary" @click="$refs.vehicleTable.refresh(true)">查询</a-button>
                <a-button type="second" style="margin-left: 10px" @click="vehicleRefresh">重置</a-button>
              </span>
            </a-col>
          </a-row>
        </a-form>
      </div>
      <div class="table-operator">
        <a-button type="primary" icon="plus" @click="vehicleAdd">新增</a-button>
        <a-button icon="delete" :disabled="!hasSelected" @click="vehicleRemove">删除</a-button>
        <a-button icon="download" :disabled="!hasSelected" @click="fileDownload">导出当前</a-button>
        <a-button icon="download" @click="filesDownload">导出所有</a-button>
        <a-button icon="upload" @click="fileUpload">导入</a-button>
      </div>
      <s-table
        ref="vehicleTable"
        size="default"
        :rowKey="record => record.id"
        :columns="columns"
        :data="loadData"
        :alert="true"
        :rowSelection="rowSelection"
        showPagination="auto"
      >
        <span slot="action" slot-scope="text, record">
          <template>
            <a @click="vehicleCheck(record)">查看</a>
            <a-divider type="vertical"/>
            <a @click="vehicleEdit(record)">编辑</a>
          </template>
        </span>
      </s-table>
    </a-card>
  </page-container>
</template>
<script>
import { STable } from '@/components'

const mockData = {
  totalCount: 1,
  totalPage: 20,
  pageNo: 1,
  pageSize: 10,
  data: [
    {
      test1: '测试1',
      test2: '测试2'
    }
  ]
}
const mockColumns = [
  {
    title: '类目1',
    dataIndex: 'test1'
  },
  {
    title: '类目2',
    dataIndex: 'test2'
  },
  {
    title: '操作',
    dataIndex: 'action',
    width: '150px',
    align: 'center',
    scopedSlots: { customRender: 'action' }
  }
]

export default {
  components: {
    STable
  },
  data () {
    return {
      selectedRowKeys: [],
      selectedRows: [],
      loadData: parameter => {
        return new Promise(resolve => {
          resolve(mockData)
        })
      }
    }
  },
  computed: {
    columns: () => mockColumns,
    hasSelected () {
      return this.selectedRowKeys.length > 0
    },
    rowSelection () {
      return {
        selectedRowKeys: this.selectedRowKeys,
        onChange: this.onSelectChange
      }
    }
  },
  methods: {
    onSelectChange (selectedRowKeys, selectedRows) {
      this.selectedRowKeys = selectedRowKeys
      this.selectedRows = selectedRows
    },
    vehicleRefresh () {
      this.$message.success('重置')
      this.$refs.table.refresh(true)
    },
    vehicleAdd () {
      // this.$message.success('新增')
    },
    vehicleRemove () {
      this.$message.success('删除')
    },
    vehicleCheck (record) {
      // this.$message.success('查看')
    },
    vehicleEdit (record) {
      // this.$message.success('编辑')
    },
    fileDownload () {
      this.$message.success('导出当前文件')
    },
    filesDownload () {
      this.$message.success('导出所有文件')
    },
    fileUpload () {
      this.$message.success('上传文件')
    }
  }
}
</script>
