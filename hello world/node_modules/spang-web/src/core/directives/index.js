import action from './action'
import tableHeight from './vTableHeight'

// 自定义指令
const directives = {
  action,
  tableHeight
}

export default {
  install(Vue) {
    Object.keys(directives).forEach((key) => {
      Vue.directive(key, directives[key])
    })
  }
}